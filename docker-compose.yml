version: '3'
services:
    discord:
      container_name: template-bot
      build:
        context: .
        dockerfile: ./Dockerfile
      restart: always
      environment:
      - NODE_ENV=production